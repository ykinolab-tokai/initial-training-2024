The MKDIR command creates one or more directories specified by the directory parameter.The MKDIR command creates one or more directories specified by the directory parameter.The MKDIR command creates one or more directories specified by the directory parameter.
